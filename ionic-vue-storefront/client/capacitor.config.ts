import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ionicvuestorefront',
  appName: 'Ionic Vue Storefront',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;